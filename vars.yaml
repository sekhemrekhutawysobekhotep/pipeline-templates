
variables:
  - name: favoriteVeggie
    value: 'brussels sprouts'
  - name: environment
    value: ${{ parameters.environment }}

parameters:
  - name: environment
    displayName: Environment
    type: string
    default: dev
    values:
      - dev
      - stg

